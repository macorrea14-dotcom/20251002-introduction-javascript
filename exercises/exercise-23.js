const numeros = [3, 5, 7, 9, 11];

const encontrarNumero = numeros.find(numero => numero > 6);

if (numero => numero > 6) {
    console.log("Encontrado " + encontrarNumero);
} else {
    console.log("No encontrado");
}