const colores = ["rojo", "azul", "verde"]; //arreglo de colores

colores.forEach((color) => { //usamos el metodo .forEach() para recorrer cada elemento del arreglo
    console.log("Color: "+color); //muestra cada color
});

//justificacion: use el método .forEach() para recorrer el arreglo y mostrar cada elemento