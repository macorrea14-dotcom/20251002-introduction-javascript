const numero = 2; //variable numero
let parImpar = (numero % 2 === 0) ? 'Par' : 'Impar'; //en esta variable use el % para saber se el numero es divisible en 2
console.log(parImpar);

//justificaion: use el operador ternario para saber si el numero es par o impar